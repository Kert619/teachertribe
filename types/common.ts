export interface BaseUser {
  id: number;
  name: string;
  email: string;
}

export interface BaseExamType {
  id: number;
  examType: string;
}

export interface BaseProblemType {
  id: number;
  problemType: string;
}
